{% include 'default/_form.html.twig' with {
    page_title: 'title.tag_edit'|trans({'%slug%': tag.slug|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'tag_index',
} %}
