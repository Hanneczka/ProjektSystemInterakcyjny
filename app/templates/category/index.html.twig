{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title.category_list'|trans }}
{% endblock %}

{% block body %}
    <p><a href="{{ path('element_index') }}">
            {{ 'title.record_list'|trans }}
        </a></p>
    <p><a href="{{ path('category_index') }}">
            {{ 'title.category_list'|trans }}
        </a></p>
    <p><a href="{{ path('tag_index') }}">
            {{ 'title.tag_list'|trans }}
        </a></p>
    <h1>{{ 'title.category_list'|trans }}</h1>
    {% if pagination is defined and pagination.items|length %}
    <div class="navigation text-center">
        {{ knp_pagination_render(pagination) }}
    </div>

    <table border="1">
        <thread>
        <tr>
            <th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 'category.id') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'label.category'|trans, 'category.name') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'label.created_at'|trans, 'category.createdAt') }}</th>
            <th>{{ 'label.actions'|trans }}</th>
        </tr>
        </thread>
        <tbody>
        {% for category in pagination %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>{{ category.createdAt|format_date('long') }}</td>
                <td><a href="{{ path('category_view', {id: category.id}) }}">
                        {{ 'action.view'|trans }}
                    </a></td>
                <td><a href="{{ path('category_edit', {id: category.id}) }}">
                        {{ 'action.edit'|trans }}
                    </a></td>
                <td><a href="{{ path('category_delete', {id: category.id}) }}">
                        {{ 'action.delete'|trans }}
                    </a></td>
            </tr>
        {% else %}
            <tr>
                {{ 'message.empty_list'|trans }}
            </tr>
        {% endfor %}
        </tbody>
    </table>
        <div class="navigation text-center mt-3">
            {{ knp_pagination_render(pagination) }}
        </div>
    {% else %}
        {{ 'message.empty_list'|trans }}
    {% endif %}
    <a href="{{ path('category_create') }}">{{ 'title.category_create'|trans }}</a>

{% endblock %}
